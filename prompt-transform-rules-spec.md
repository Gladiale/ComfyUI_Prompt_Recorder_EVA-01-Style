# 総括欄プロンプト変換ルール機能 仕様書

## 1. 概要

総括欄（`SynthesisPanel`）に表示されるプロンプトへ、ユーザーが作成した文字列変換ルールを適用できる機能を追加する。

ワードツリー内の元ワード本文は変更せず、総括欄への表示内容とコピー内容だけを変換する。

## 2. 対象範囲

### 2.1 変換対象

- 総括欄に表示されるプロンプト
- 総括欄のコピーボタンでクリップボードへ送る内容
- 差分検知用スナップショットに保存される出力テキスト

### 2.2 変換対象外

- ワードツリー内の元ワード本文
- ワードの注釈
- プリセットの元データ
- `PresetEntry.text`
- 選択状態
- ワードに設定された強度値

## 3. データモデル

### 3.1 `PromptTransformRule`

```ts
export interface PromptTransformRule {
  id: string;
  name: string;
  from: string;
  to: string;
  enabled: boolean;
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---:|---|
| `id` | `string` | ○ | ルール識別用の一意ID |
| `name` | `string` | ○ | ルール名 |
| `from` | `string` | ○ | 変換前の文字列 |
| `to` | `string` | ○ | 変換後の文字列。空文字を許可 |
| `enabled` | `boolean` | ○ | ルール適用状態 |

### 3.2 `RootState` の変更

```ts
export interface RootState {
  version: number;
  rootGroups: Group[];
  presets?: PromptPreset[];
  rules: PromptTransformRule[];
}
```

### 3.3 バージョン

```ts
export const ROOT_VERSION = 2;
```

旧バージョンの状態を読み込んだ場合は、`rules: []` を追加して `version: 2` に正規化する。

## 4. ルール入力仕様

### 4.1 入力項目

| 項目 | UI | 必須 |
|---|---|---:|---:|
| ルール名 | 1行入力 | ○ |
| 変換前の文字 | 1行入力 | ○ |
| 変換後の文字 | 1行入力 | × |

### 4.2 入力値の正規化

保存時に、ルール名・変換前・変換後のすべてへ `trim()` を適用する。変換後が空文字の場合は、該当文字列を削除するルールとして扱う。

### 4.3 許可する文字

文字を制限しない。

### 4.4 バリデーション

以下の場合は保存できない。

- ルール名が空
- 変換前の文字が空

変換後の文字が空であることはエラーにしない。エラーは各入力欄の直下へ表示し、エラー中は保存ボタンを無効化する。

## 5. 変換ルールの適用

### 5.1 適用対象

各選択ワードの本文へルールを適用する。強度表記はルール適用後に付与する。

### 5.2 適用順序

`enabled === true` のルールだけを、ルール一覧の上から順番に逐次適用する。ドラッグ&ドロップによる並び順が、そのまま適用順となる。

例：

```text
入力: A
ルール1: A → B
ルール2: B → C
結果: C
```

### 5.3 変換処理手順

各ワードについて、以下の順で処理する。

1. 元ワード本文を `trim`
2. 有効ルールを一覧順に適用
3. 全ルール適用後の結果を `trim`
4. 変換後本文が空文字の場合は表示用出力から除外
5. 変換後本文へ元ワードの強度を付与
6. 変換後の強度付き文字列で重複排除

### 5.4 文字列置換仕様

変換前の文字列は正規表現ではなく、通常のリテラル文字列として扱う。

- 大文字・小文字を区別する
- 記号を特別扱いしない
- 正規表現構文を解釈しない
- 変換後の `$` などを置換記号として解釈しない
- 一致箇所をすべて置換する
- 重複する一致は左から順番に非重複で置換する
- 各ルールは1回だけ適用する
- 自己置換・循環置換を許可する

変換後文字列は置換テンプレートではなく、完全なリテラルとして扱う。

### 5.5 空白処理

ルール間では空白を除去しない。全ルール適用後に1回だけ `trim()` を適用する。

```text
元文字列: cat
ルール1: cat → "  dog  "
ルール2: dog → animal
途中結果: cat → "  dog  " → "  animal  "
最終結果: animal
```

## 6. 重複排除

### 6.1 表示用重複排除

変換後の強度付き文字列を対象に、既存の `normalizeText` を使用して重複排除する。

`normalizeText` の処理は、前後空白の除去、小文字化、連続空白の1つへの圧縮とする。最初に出現したワードを残す。

### 6.2 差分用データ

表示用の重複排除済み配列とは別に、元ワード単位の差分用エントリを保持する。重複排除で表示から除外されたワードも差分追跡する。

### 6.3 区切り文字

表示用エントリを現在の区切り設定で結合する。

- カンマ区切り：`, `
- 改行区切り：`\n`

## 7. 強度表記

ルールはワード本文へ適用し、強度表記は変換後に付与する。

```text
元ワード: cat
強度: 2
ルール: cat → dog
出力: (dog:1.1)
```

## 8. 総括欄の表示

## 9. ルール調整UI

### 9.1 ボタンA

`SynthesisPanel` の区切り切替ボタンの隣に追加する。

- アイコン：`FiSliders`
- テキストラベル：なし
- `title`：変換ルールを調整
- 有効ルールが1件以上ある場合は発光
- 発光表現は差分検知ボタンと同様とする

### 9.2 ポップアップ

`DiffPopup` と同様の右寄せポップアップとして表示する。

- ヘッダー右側から下方向へ展開
- 固定幅：約380px
- 最大高さを設定
- 一覧部分は内部スクロール
- 右上にルール追加ボタン

### 9.3 ポップアップの開閉

- 外側クリック：閉じる

## 10. ルール一覧

### 10.1 表示項目

各行に以下を表示する。

- ルール名
- 変換前
- 変換後
- 適用状態
- 適用／解除ボタン
- 編集ボタン
- 削除ボタン

### 10.2 適用状態

| 状態 | ボタン表示 |
|---|---|
| 無効 | 適用 |
| 有効 | 解除 |

有効な行は色変更、枠線または背景色変更、発光などで強調する。

### 10.3 編集

編集ボタンを押すと、一覧上部にフォームを展開し、既存値を入力済みにする。適用状態は維持する。保存後はフォームを閉じて一覧へ戻る。

### 10.4 削除

削除前に既存の `ConfirmDialog` を表示する。確認内容にはルール名、変換前、変換後を含める。

## 11. ルール並べ替え

### 11.1 操作方式

ドラッグ&ドロップを使用する。

- ルール行全体をドラッグ元とする
- マウス操作に対応する
- タッチ・ペン用の専用対応は行わない
- 上下移動ボタンは追加しない

### 11.2 操作ボタンとの干渉

適用／解除、編集、削除ボタンからはドラッグを開始しない。

### 11.3 ドロップ位置

ドロップ先のルール行の前へ挿入する。

```text
変更前:
A
B
C

CをAへドロップ後:
C
A
B
```

## 12. 差分検知

### 12.1 基本方針

コピー時点の変換後テキストをスナップショットへ保存する。ルール一覧・順序・適用状態そのものは保存しない。ルール変更後に実際の出力テキストが変わった場合のみ差分とする。

### 12.2 差分用エントリ

差分用エントリは、元本文が空でない選択ワードを重複に関係なく全件保持する。変換後に空文字になっても保持し、元ワードID単位で管理する。

### 12.3 テキスト比較

テキスト変更の判定は完全一致で行う。大文字・小文字、前後空白、内部空白、改行、記号、変換後文字列の違いを検出する。

### 12.4 空文字化

コピー時の `cat` が、ルール変更後に空文字になった場合は、以下のように表示する。

```text
テキスト変更
cat → （空欄）
```

選択状態は維持されているため、「削除」ではなく「テキスト変更」とする。

## 13. Import／Export

### 13.1 Export

Export JSONには常に `rules` フィールドを含める。ルールが0件の場合も `rules: []` を出力する。

```json
{
  "version": 2,
  "rootGroups": [],
  "presets": [],
  "rules": []
}
```

### 13.2 Import

Import時は、ワードツリー、プリセット、変換ルールを一括置換する。現在のルールはImportデータ内のルールで置き換える。`rules` がない場合は空配列として扱う。

### 13.3 ルール正規化

| 条件 | 処理 |
|---|---|
| `rules` が配列でない | 空配列 |
| `name` が文字列でない | ルール除外 |
| `name` が空 | ルール除外 |
| `from` が文字列でない | ルール除外 |
| `from` が空 | ルール除外 |
| `to` が文字列でない | 空文字として扱う |
| `id` が欠損・不正 | 新しいIDを生成 |
| `enabled` が真偽値でない | `false` |

### 13.4 ID重複

同一Importデータ内でIDが重複している場合、最初のルールのIDは維持し、2件目以降には新しいIDを生成する。その他のフィールドは維持する。

### 13.5 Import時の適用状態

`enabled` が欠損または真偽値以外の場合は無効にする。旧形式データや `rules` が存在しないデータはルールなしとして扱う。

## 14. 永続化

ルールは `RootState` の一部として、既存の `chrome.storage.local` への状態保存処理で永続化する。

対象操作：

- ルール追加
- ルール編集
- ルール削除
- 適用状態変更
- ルール並べ替え
- Import

操作後は既存のdebounce保存を利用する。ブラウザ再起動後もルールを復元する。

## 15. 旧スナップショット

機能追加前の旧形式スナップショットは利用しない。旧形式を読み込んだ場合は破棄し、次回コピー時に変換後テキストを含む新形式のスナップショットを作成する。

## 16. 初期状態

```ts
{
  version: 2,
  rootGroups: [],
  rules: []
}
```

新規作成したルールは必ず無効状態で登録する。

```ts
{
  id: "...",
  name: "...",
  from: "...",
  to: "...",
  enabled: false
}
```

## 17. 受け入れ条件

### 17.1 ルール作成

- [ ] ルール名、変換前、変換後を入力できる
- [ ] ルール名と変換前が必須として検証される
- [ ] 変換後を空にできる
- [ ] 保存後に一覧へ戻る

### 18.2 変換

- [ ] 有効ルールだけが適用される
- [ ] 一覧順に逐次適用される
- [ ] 大文字・小文字を区別する
- [ ] 正規表現として解釈されない
- [ ] `$` がリテラルとして出力される
- [ ] 変換後に空文字になったワードが表示から除外される
- [ ] 強度表記が変換後に付与される
- [ ] 変換後の重複が除去される

### 18.3 UI

- [ ] `FiSliders` ボタンが表示される
- [ ] 有効ルールがある場合にボタンが発光する
- [ ] ルール調整ポップアップを開閉できる
- [ ] 追加・編集フォームを展開できる
- [ ] ルールを適用・解除できる
- [ ] ルールを削除できる
- [ ] 削除前に確認ダイアログが表示される
- [ ] ルールをドラッグ&ドロップで並べ替えられる
- [ ] 操作ボタンからドラッグが開始されない

### 18.4 差分検知

- [ ] コピー時の変換後テキストが保存される
- [ ] ルール変更によるテキスト変更が検出される
- [ ] 変換後の空文字化が `テキスト変更` として表示される
- [ ] 重複排除で非表示になったワードも追跡される

### 18.5 Import／Export

- [ ] Export JSONに常に `rules` が含まれる
- [ ] ルール0件でも `rules: []` が出力される
- [ ] Importでルールが置換される
- [ ] 旧形式Importで `rules: []` になる
- [ ] 不正ルールが除外される
- [ ] 正常なルールは読み込まれる
- [ ] 重複IDが自動修正される
- [ ] Import後にルールが永続化される
